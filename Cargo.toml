[package]
name = "proc"
version = "0.1.0"
edition = "2024"

[dependencies]
proc_attribute = { version = "0.1.0", path = "attribute" }
proc_derive = { version = "0.1.0", path = "derive" }
proc_parse = { version = "0.1.0", path = "parse" }
proc_common = { version = "0.1.0", path = "common" }

[workspace]
members = ["attribute", "common", "derive", "parse"]
