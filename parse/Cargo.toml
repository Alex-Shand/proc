[package]
name = "proc_parse"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
proc_derive = { version = "0.1.0", path = "../derive" }
proc_common = { version = "0.1.0", path = "../common" }

[dev-dependencies]
trybuild = "1.0.104"
