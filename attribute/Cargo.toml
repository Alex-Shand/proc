[package]
name = "proc_attribute"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
proc_common = { version = "0.1.0", path = "../common" }

[dev-dependencies]
macrotest = "1.2.0"
trybuild = "1.0.115"
